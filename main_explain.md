# Объяснение кода main.py

## 🚀 Как запустить?

```bash
# Просто запустите программу - она сама установит нужные библиотеки!
python main.py
```

**Важно:** Программа автоматически проверит и установит все необходимые библиотеки, если их нет в системе. Вам не нужно ничего устанавливать вручную!

Программа откроет окно с 3D графиком, который будет вращаться, показывая плоскость решения со всех сторон.

## 📚 Что делает эта программа?

Эта программа демонстрирует работу **машинного обучения** на простом примере. Она учит компьютер различать "радостные" и "грустные" предложения, а затем визуализирует результат в виде 3D графика с анимацией.

## 🎯 Задача программы

Программа решает задачу **классификации** (разделения на классы):
- **Входные данные**: предложения из слов "аак", "бик", "хрясь"
- **Задача**: определить, радостное предложение или грустное
- **Выход**: визуализация в 3D с вращающейся плоскостью, которая разделяет классы

## 📖 Пошаговое объяснение кода

### Шаг 1: Проверка и установка библиотек

```python
def check_and_install_package(package_name, import_name=None):
    try:
        __import__(import_name)
        print(f"✅ Библиотека '{package_name}' уже установлена")
    except ImportError:
        # Устанавливаем библиотеку
        subprocess.check_call([sys.executable, "-m", "pip", "install", package_name])
```

**Что это значит?**
Программа автоматически проверяет, установлены ли нужные библиотеки:
- Если библиотека есть → продолжает работу
- Если библиотеки нет → автоматически устанавливает её через pip

**Почему это удобно?**
Не нужно вручную устанавливать библиотеки перед запуском программы. Программа сама всё сделает!

**Аналогия**: Это как автоматическая проверка инструментов перед работой - если чего-то не хватает, программа сама достанет нужное.

---

### Шаг 2: Импорт библиотек

```python
from sklearn.linear_model import LogisticRegression
import matplotlib.pyplot as plt
from mpl_toolkits.mplot3d import Axes3D
import numpy as np
from matplotlib import animation
```

**Что это значит?**
- `sklearn` - библиотека для машинного обучения
- `matplotlib` - библиотека для создания графиков
- `numpy` - библиотека для работы с массивами чисел
- `animation` - модуль для создания анимации

**Аналогия**: Это как взять инструменты из ящика - мы берем то, что нам нужно для работы.

---

### Шаг 3: Подготовка данных

```python
train_sentences = [
    "аак аак аак",  # радостно
    "бик бик",      # грустно
    ...
]
y = [1, 1, 1, 0, 0, 0, ...]
```

**Что это значит?**
- `train_sentences` - список предложений для обучения
- `y` - правильные ответы: `1` = радостно, `0` = грустно

**Аналогия**: Это как учебник с вопросами и ответами. Мы показываем модели примеры и правильные ответы, чтобы она научилась.

---

### Шаг 4: Извлечение признаков

```python
def extract_features(sentence: str):
    words = sentence.split()
    count_aak = sum(1 for w in words if w == "аак")
    count_bik = sum(1 for w in words if w == "бик")
    count_hrjas = sum(1 for w in words if w == "хрясь")
    return [count_aak, count_bik, count_hrjas]
```

**Что это значит?**
Функция преобразует предложение в числа:
- Берет предложение: `"аак аак бик"`
- Считает слова: 2 "аак", 1 "бик", 0 "хрясь"
- Возвращает: `[2, 1, 0]`

**Почему это нужно?**
Компьютер не понимает слова, он понимает только числа. Поэтому мы преобразуем слова в числа.

**Аналогия**: Это как переводчик - переводит слова на "язык чисел", который понимает компьютер.

---

### Шаг 5: Обучение модели

```python
X = np.array([extract_features(s) for s in train_sentences])
model = LogisticRegression()
model.fit(X, y)
```

**Что это значит?**
1. Преобразуем все предложения в числа: `X`
2. Создаем пустую модель
3. Обучаем модель: `model.fit(X, y)`

**Что происходит при обучении?**
Модель ищет закономерности:
- Если много "аак" → радостно
- Если много "бик" → грустно
- И так далее...

**Аналогия**: Это как учитель объясняет ученику правила. После обучения модель "запомнила" эти правила.

---

### Шаг 6: Создание 3D графика

```python
fig = plt.figure(figsize=(8, 7))
ax = fig.add_subplot(111, projection="3d")
```

**Что это значит?**
- Создаем окно для графика размером 8x7 дюймов
- Создаем 3D систему координат (как в геометрии: X, Y, Z)

**Аналогия**: Это как подготовить лист бумаги и нарисовать на нем оси координат.

---

### Шаг 7: Рисование точек

```python
colors = ["green" if label == 1 else "red" for label in y]
ax.scatter(X_aak, X_bik, X_hrjas, c=colors, s=60)
```

**Что это значит?**
- Каждое предложение становится точкой в 3D пространстве
- Координаты точки = количество каждого слова
- Зеленые точки = радостные предложения
- Красные точки = грустные предложения

**Пример:**
- Предложение `"аак аак бик"` → `[2, 1, 0]` → точка с координатами (2, 1, 0)

**Аналогия**: Это как отметить на карте города разными цветами - зеленые = хорошие, красные = плохие.

---

### Шаг 8: Рисование плоскости решения

```python
w0 = model.intercept_[0]
w1, w2, w3 = model.coef_[0]
zz = (-w0 - w1 * xx - w2 * yy) / (w3 + 1e-9)
ax.plot_surface(xx, yy, zz, alpha=0.3, color="blue")
```

**Что это значит?**
- Модель находит "плоскость", которая разделяет зеленые и красные точки
- Плоскость - это граница между "радостно" и "грустно"
- Формула плоскости: `w0 + w1*x + w2*y + w3*z = 0`

**Аналогия**: Это как стена, которая разделяет два класса. Все зеленые точки с одной стороны, красные - с другой.

---

### Шаг 9: Анимация вращения

```python
def animate(i):
    ax.view_init(elev=20, azim=i)
    return fig,

anim = animation.FuncAnimation(
    fig, animate, init_func=init,
    frames=360, interval=30
)
```

**Что это значит?**
- Функция `animate()` вызывается 360 раз (по одному разу на каждый градус)
- Каждый раз меняется угол обзора (`azim=i`)
- Результат: график вращается на 360 градусов

**Аналогия**: Это как вращать глобус, чтобы рассмотреть его со всех сторон.

---

## 🔑 Ключевые понятия

### 1. Признаки (Features)
Числа, которые описывают объект. В нашем случае: количество каждого слова.

### 2. Модель (Model)
Алгоритм, который учится на данных и делает предсказания.

### 3. Обучение (Training)
Процесс, когда модель изучает данные и находит закономерности.

### 4. Классификация (Classification)
Разделение объектов на классы (в нашем случае: радостно/грустно).

### 5. Плоскость решения (Decision Boundary)
Граница, которая разделяет разные классы.

---

## 💡 Как это работает в реальной жизни?

Подобные алгоритмы используются везде:

1. **Спам-фильтры** - определяют, спам письмо или нет
2. **Распознавание изображений** - определяют, что на картинке
3. **Рекомендации** - предлагают фильмы, музыку, товары
4. **Медицина** - помогают диагностировать болезни

---

## 🎓 Что можно улучшить?

1. **Больше данных** - чем больше примеров, тем лучше модель
2. **Больше признаков** - можно добавить длину предложения, порядок слов и т.д.
3. **Другие алгоритмы** - можно попробовать нейронные сети, деревья решений
4. **Валидация** - проверить модель на новых данных, которых она не видела

---



## 📝 Структура программы

```
1. Проверка и установка библиотек (автоматически)
   ↓
2. Импорт библиотек
   ↓
3. Подготовка данных (предложения и метки)
   ↓
4. Извлечение признаков (преобразование в числа)
   ↓
5. Обучение модели
   ↓
6. Создание 3D графика
   ↓
7. Рисование точек (зеленые/красные)
   ↓
8. Рисование плоскости решения
   ↓
9. Создание анимации вращения
   ↓
10. Отображение графика
```

---

## ❓ Частые вопросы

**Q: Почему используются именно эти слова "аак", "бик", "хрясь"?**  
A: Это просто пример. В реальности можно использовать любые слова или признаки.

**Q: Что если модель ошибется?**  
A: Это нормально! Модель учится на данных, и если данных мало или они неточные, модель может ошибаться. Нужно больше данных для лучшей точности.

**Q: Можно ли использовать другие алгоритмы?**  
A: Да! Можно попробовать другие алгоритмы из sklearn: DecisionTreeClassifier, RandomForestClassifier, SVM и т.д.

**Q: Что означает "плоскость решения"?**  
A: Это математическая граница, которая разделяет два класса. Все точки с одной стороны плоскости - один класс, с другой стороны - другой класс.

---

**Удачи в изучении машинного обучения! 🎉**

